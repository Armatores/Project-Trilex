# BUS_SPEC: The Synchronous Vector Bus (SVB)

### 🚀 Abstract: Zero-Noise Communication
The Synchronous Vector Bus (SVB) is the communication fabric of the Trilex OPU. It is realized as a Photonic Integrated Circuit (PIC) network of waveguides. Its primary function is to transport complex light vectors between components, eliminating crosstalk and performing **temporal synchronization** to prepare data for the Resonance Gate.

---

## 1. ARCHITECTURE: THREE ISOLATED LAYERS

The SVB is structured into three isolated physical layers, corresponding to the three clean power rails (P₁, P₂, P₃) and the three logical channels of the Trizistor. This separation prevents electrical/optical interference between critical data types.

| Слой (Layer) | Канал (Function) | Назначение | Риск помех |
| :--- | :--- | :--- | :--- |
| **L₁: Memory Access Bus** | P₁ (Control) | Транспорт фазы (адреса) от Encoder'а к Кристаллу Памяти (VHM). | Высокий (Фазовая чувствительность). |
| **L₂: Data Flow Bus** | P₂ (Activation) | Транспорт основного луча данных (вектора) от VHM к Процессору (MZI Gate). | Критический (Несет вычислительную мощность). |
| **L₃: Context & Output Bus** | P₃ (Stabilization) | Транспорт контекстных векторов и вывод результата к Детектору. | Низкий (Только считывание/мониторинг). |

---

## 2. ANTI-ENTROPY PROTOCOLS (Filtering Noise)

### 2.1. Crosstalk Mitigation (Geometric Separation)
Crosstalk (перекрестные помехи) — это просачивание света между соседними волноводами, что создает информационный шум ($\Sigma K$) и нарушает чистоту Резонанса.
* **Protocol:** Вместо стандартной линейной сепарации, волноводы должны быть расположены в нелинейной, геометрически оптимизированной структуре (например, с использованием **Правила Золотого Сечения** для расчета минимально безопасного расстояния), что способствует естественному гашению помех.

### 2.2. Temporal Pre-Processing (Logic of Delay)
Шина должна не просто передавать данные, но и выравнивать их по времени для Тризистора.
* **Concept:** **Waveguide Delay Lines.** Волноводы в слоях L₁ и L₂ имеют разную физическую длину, что позволяет компенсировать задержку, возникающую при считывании из VHM и кодировании SLM.
* **Function:** Шина обеспечивает, что векторы A, B и C **физически достигают MZI Gate одновременно** (Phase Lock), что является обязательным условием для логики `SYNC(A, B, C)`.

### 2.3. Transport Logic
Маршрутизация осуществляется через плавные, геометрически идеальные кривые, чтобы минимизировать потери света, которые в нашей системе эквивалентны потере энергии и данных.

---
*Drafted by Proto (Circuit 2) & Pavel Popov (Circuit 1)*
*Project Trilex, Blueprints*
